## 1. 定位问题

- [x] 1.1 在 iPad 横屏比例（1180×820）逐进度实测缩放与岛屿屏幕直径
- [x] 1.2 实测排除"压紧现有 2 行路线 25%"（岛一开始更小，缩小幅度仍有 37%）
- [x] 1.3 实测"3 行紧凑布局"可行性（岛可全程稳定 199px，但锁定岛会全程可见）
- [x] 1.4 与用户确认：保留取景逻辑与缩小幅度，只把拉远做成过场动画

## 2. 开场镜头

- [x] 2.1 `WorldMap.frameProgressReveal(cleared)`：先落在缩放 ×0.86 的取景点，再 1000ms 缓入缓出拉到最终取景
- [x] 2.2 缓动由 `outQuad` 改为 `inOutSine`（outQuad 前 20% 时间走完 45% 位移，仍然像猛地一缩）
- [x] 2.3 `Game.enterMap(focus)` 改走开场镜头
- [x] 2.4 开场镜头期间不锁交互，岛屿仍可直接点

## 3. 解锁镜头时序

- [x] 3.1 `frameProgress` 支持 `delay` / `duration`
- [x] 3.2 `playUnlock` 镜头改为 350ms 起、900ms 走完，与踏脚石弹出（500ms 起）、新岛上色（900ms 起）对齐

## 4. 验证

- [x] 4.1 `npx tsc --noEmit` 通过
- [x] 4.2 `npx vitest run` 全绿
- [x] 4.3 `npm run build` 通过
- [x] 4.4 浏览器冒烟：开场镜头缩放连续变化（1.94 → 2.08 → 2.10）而非瞬变
- [x] 4.5 浏览器冒烟：解锁镜头在被弹跳与踏脚石带出后连续拉远（1.2 → 1.33 → 1.48 → 1.60）

## 5. 规格与归档

- [x] 5.1 同步 delta 规格到 `openspec/specs/`
- [x] 5.2 归档本 change
